<!doctype html>
<html lang="pl">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ site.name }}</title>
    <style>
      body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, Arial, sans-serif; max-width: 780px; margin: 2rem auto; padding: 0 1rem; }
      header { margin-bottom: 1rem; }
      .post { margin: .5rem 0; }
      a { text-decoration: none; }
      a:hover { text-decoration: underline; }
      footer { margin-top: 2rem; color: #666; font-size: .9rem; }
      .rss { float: right; }
    </style>
  </head>
  <body>
    <header>
      <h1>{{ site.name }}</h1>
      {% if site.enable_rss %}<a class="rss" href="/rss.xml">RSS</a>{% endif %}
      <p>Automatycznie publikowane ciekawostki z danych Wikidata.</p>
    </header>
    <main>
      <h2>Najnowsze wpisy</h2>
      <ul>
        {% for p in posts %}
          <li class="post"><a href="/posts/{{ p | replace('.md', '.html') }}">{{ p[11:-3] | replace('-', ' ') }}</a></li>
        {% else %}
          <li>Brak wpisów — pipeline opublikuje pierwsze wkrótce.</li>
        {% endfor %}
      </ul>
    </main>
    <footer>
      <p>© {{ site.name }}. Dane: Wikidata (CC0). Ten serwis jest generowany automatycznie.</p>
    </footer>
  </body>
</html>
